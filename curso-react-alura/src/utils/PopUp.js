

const PopUp = {
    exibeMensagem : (status, msg) =>{

        if(status === "success"){
            
        }

        if(status === 'error'){
            
        }

    }
}

export default PopUp;